<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content, class: "mb-2" %>
  <%= f.input :rating, as: :select, collection: 1..5 %>
  <%= f.submit "Submit review", class: "btn btn-primary mt-3" %>
<% end %>
